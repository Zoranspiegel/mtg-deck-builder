import { Text, View } from 'react-native';
import { styles } from './DecksScreen.styles';

export const DecksScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={{ color: 'white' }}>DECKS</Text>
    </View>
  );
};
